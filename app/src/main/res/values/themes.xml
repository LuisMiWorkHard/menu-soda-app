<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema asignado en el Manifest. Hereda de Theme.SplashScreen para que la API funcione.
         postSplashScreenTheme restaura el tema real de la app tras el primer frame. -->
    <style name="Theme.MenuApp" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.MenuApp.App</item>
    </style>

    <!-- Tema de la app en ejecución (equivalente al Theme.MenuApp original) -->
    <style name="Theme.MenuApp.App" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
